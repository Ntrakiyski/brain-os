- In tools to build folders for example memory tools will be a folderand each memory tool will be unique file separate file I want to follow this concept because I will have many tools in the future it's the same for database there is queries now 1 fileSo I want to have a folder which is called queries and then all the queries inside in a separate fileand I don't knowanywhere else this need to be implemented was but let's follow this approach you can update the cloud dot MD so that it follows this idea of building